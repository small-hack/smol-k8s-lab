# don't check in the config.yml as that may have private info
config.yml
.env

# ignore python cache
**/__pycache__

# ignore apple finder stuff
**/.DS_Store

# when we do brew installs with a bundle file, we get this
deps/Brewfile.lock.json

# python building
smol_k8s_lab.egg-info/**
dist/**
build/**
